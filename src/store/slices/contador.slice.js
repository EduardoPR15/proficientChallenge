import { createSlice } from "@reduxjs/toolkit";

export const contadorSlice = createSlice({
    name: 'contador',
    initialState: 0,
    reducers:{

    }
})

export const {} = contadorSlice.actions

export default contadorSlice.reducer